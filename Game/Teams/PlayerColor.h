#pragma once 

enum class PlayerColor {
  // Has to be, because a logic operation used in Player.cpp respectively for 0 and 1
  // Just for readibility purposes
  red = 0,
  blue,
  unkown
};
